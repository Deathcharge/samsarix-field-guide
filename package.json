{
  "name": "helix-ecosystem-website",
  "version": "1.0.0",
  "private": true,
  "description": "A dependency-free public field guide to the Helix repository ecosystem.",
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "lint": "node scripts/lint.mjs",
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "check": "npm run lint && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "SEE LICENSE IN LICENSE"
}
